/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * To learn more about Gradle by exploring our Samples at https://docs.gradle.org/8.5/samples
 * This project uses @Incubating APIs which are subject to change.
 */

task firstTask {
    doFirst {
        println ("This will be printed first!")
    }

    doLast {
        println "This will be printed last!"
    }
}

/* It's required to use doFirst and doLast closures, as they will ensure which one runs at what time
    also, not using them will run the task at time of build
*/

task secondTask {
    doLast {
        println("trying out different tasks!")
    }
}